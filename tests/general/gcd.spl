/*
 * Determines the greatest common divisor for positive integers a and b.
 */
Int gcd (Int a, Int b) {
    if (a < b)
        return gcd(b, a);
    else if (b == 0)
        return a;
    return gcd(b, a % b);
}

Void main () {}
